server:
  port: ${PORT:8080}   # Render هيديك البورت تلقائي

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

openai:
  api-key: ${OPENAI_API_KEY}   # مش هنحط المفتاح هنا