server:
  port: ${PORT:8080}

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

openai:
  api-key: ${OPENAI_API_KEY}

chroma:
  url: ${CHROMA_URL:http://localhost:8000}